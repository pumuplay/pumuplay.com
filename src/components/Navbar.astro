---
import { Search } from 'lucide-react';
---

<nav class="flex items-center justify-between py-6 px-6 max-w-7xl mx-auto">
  <div class="flex items-center gap-2">
    <img src="/logo.png" alt="PumuPlay Logo" class="w-8 h-8 rounded-full" />
    <span class="font-bold text-lg tracking-tight">PumuPlay</span>
  </div>

  <div class="hidden md:flex items-center gap-8 text-sm font-medium text-gray-600">
    {
      [
        { name: 'Home', href: '/' },
        { name: 'Projects', href: '/projects' },
        { name: 'Blogs', href: '/blogs' },
        { name: 'About Us', href: '/about' },
      ].map((link) => {
        const isActive = Astro.url.pathname === link.href || (link.href !== '/' && Astro.url.pathname.startsWith(link.href));
        return (
          <a
            href={link.href}
            class={isActive ? "text-secondary" : "hover:text-black transition-colors"}
          >
            {link.name}
          </a>
        );
      })
    }
      
  </div>

  <!-- <div class="flex items-center gap-4">
    <button class="p-2 hover:bg-gray-200 rounded-full transition-colors" aria-label="Search">
      <Search size={20} />
    </button>
    <a href="#" class="px-5 py-2 border border-gray-300 rounded-full text-sm font-semibold hover:bg-black hover:text-white transition-colors">
      Sign In
    </a>
  </div> -->
</nav>

<script>
  // Mobile menu toggle logic can go here if needed later
</script>
